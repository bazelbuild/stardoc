---
matrix:
  platform:
    - ubuntu1804
    - ubuntu2004
    - macos
    - windows

tasks:
  build_and_test:
    name: Build and test
    platform: ${{ platform }}
    build_targets:
      - "//..."
    test_targets:
      - "//..."

  bzlmod:
    name: Bzlmod example
    platform: ${{ platform }}
    working_directory: test/bzlmod
    build_targets:
      - "//..."
    test_targets:
      - "//..."

buildifier: latest
